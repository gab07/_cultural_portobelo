<div class="row">
  <%= form_for @quotation, html: { multipart: true } do |f| %>

  <form class="form-control">

    <% if @quotation.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@quotation.errors.count, "error") %> prohibited this quotation from being saved:</h2>
        <ul>
          <% @quotation.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="col-md-3">
      <div class="form-group">
        <%= f.label :Publicado %>
        <%= f.check_box :published %>
      </div>
      
      <div class="form-group">
     
      </div>
    </div>

    <div class="col-md-2">
       <button type="submit" class="btn btn-default">Crear Cotización</button>
    </div>

  </form>

  <% end %>
</div>
