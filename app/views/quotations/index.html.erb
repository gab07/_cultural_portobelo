<h1 class="cart-text"><%= render 'quotations/quotation_text' %></h1>
<h3 class="text-center">Nueva cotización</h3>

<div class="row">
  <div class="col-xs-6 col-xs-offset-3">
    <% @books.each do |book| %>
      <%= render "book_row", book: book, order_item: @order_item %>
    <% end %>
  </div>
</div>