<div class="container">
	<div class="row">
		<% @quotations.each do |quotation| %>
		<div class="col-md-6">
			<%= link_to "#{quotation.created_at.to_date}", quotation_path(quotation), class: "btn btn-link" %>
		</div>
		<% end %>
	</div>
</div>